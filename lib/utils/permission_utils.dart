class PermissionUtils {
  bool checkComponentAccessibility(
      {required String checked, required bool isPermission}) {
    if (isPermission) {
      // Check if found to permissions
      return true;
    }
    // Check if found in permissions
    return true;
  }
}
